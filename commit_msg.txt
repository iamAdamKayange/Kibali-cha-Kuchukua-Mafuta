Fix Welcome/Landing page text encoding issue

ISSUE:
- Hero subtitle had corrupted quote characters (â€œ and â€)
- Caused by incorrect encoding in line 373 of page.tsx

FIX:
- Removed corrupted quote characters
- Text now renders correctly: "A digital system for requesting, approving, and managing fuel permits efficiently and transparently."
- Typography and responsive display verified

SYSTEM ANALYSIS COMPLETED:
✅ Authentication & Session Management - Working correctly
   - JWT access/refresh tokens
   - Persistent sessions (close browser, reopen)
   - /auth/me returns user with avatar field
   - Session restoration calls /auth/me

✅ Role-Based Access Control - Working correctly
   - All 6 roles have correct dashboard routing
   - roleToDashboard function handles all roles
   - Backend role scoping preserved
   - Department filtering for HEAD_OF_DEPARTMENT
   - Approval tracking via approvals.some.approverId

✅ Approval/Rejection Workflow - Working correctly
   - HEAD → TRANSPORT → ADA → PROCUREMENT stages
   - Rejection statuses: HEAD_REJECTED, TRANSPORT_REJECTED, ADA_REJECTED, CANCELLED
   - Approvers see requests they interacted with
   - HEAD_OF_DEPARTMENT status overwrite fix preserved

✅ Notifications System - Functional
   - Header displays notifications
   - Notifications page exists
   - Push notification integration in place

✅ Profile Image Synchronization - Fixed in previous commit
   - avatar field added to User interface
   - updateUser method in AuthContext
   - Header displays avatar in all pages
   - Global state updates without reload

✅ Printing Permissions - Preserved
   - Fuel Permit: PROCUREMENT + final approver
   - Full Statement: TRANSPORT_OFFICER + PROCUREMENT
   - Status requirements: FULLY_APPROVED, COMPLETED

✅ Language Support (English/Swahili) - Working correctly
   - LanguageContext with full dictionaries
   - Language switcher in Header
   - All UI text translated
   - No broken characters or encoding issues

✅ Responsive UI & PWA - Working correctly
   - PWA manifest configured
   - Service worker generated
   - Mobile-responsive design
   - Install prompt component
   - Offline notice component

✅ Build - Successful
   - Next.js build completed
   - Linting passed
   - Type checking passed
   - 35 static pages generated
   - PWA worker compiled

PRESERVED:
- All existing functionality
- Authentication/authorization
- Approval workflow
- Rejection workflow
- Notifications
- Printing permissions
- Security model
- Business rules